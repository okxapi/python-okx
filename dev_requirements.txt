# Development & Testing dependencies for python-okx.
# Installs the runtime deps plus dev/release tooling in one step:
#   pip install -r dev_requirements.txt
-r requirements.txt
python-dotenv>=1.0.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
ruff>=0.1.0
build>=1.0.0
twine>=4.0.0
